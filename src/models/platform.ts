let isLinux = process.platform === 'linux';
let isFreeBSD = process.platform === 'freebsd';
let isOSX = process.platform === 'darwin';
let isSunOS = process.platform === 'sunos';

export { isLinux, isFreeBSD, isOSX, isSunOS }
